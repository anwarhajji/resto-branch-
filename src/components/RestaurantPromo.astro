---
// RestaurantPromo.astro
import { Picture } from "astro:assets";
import HeroImage from "../assets/images/HOM1.jpg"; // Update path to your image
import GroupImage from "../assets/images/HOM2.jpg"; // Update path to your image

import slideImage1 from "~/assets/images/HOM1.jpg";
import slideImage2 from "~/assets/images/G4.jpg";
import slideImage3 from "~/assets/images/G6.jpg";
import ImageSlider from "./ImageSlider.astro";

// Prepare the data in the format required by ImageSlider's Props
const sliderData = [
  { src: slideImage1, alt: "Alt text for the first beautiful slide image" },
  { src: slideImage2, alt: "Alt text describing the second slide content" },
  {
    src: slideImage3,
    alt: "Third slide showing an amazing landscape or object",
  },
];
---

<div class="font-sans text-black overflow-hidden">
  <!-- Section Hero en pleine largeur -->
  <section class="relative w-full h-96 md:h-screen overflow-hidden">
    <!-- Image Hero avec composant Picture -->
    <div class="absolute inset-0 z-0">
      <ImageSlider
        images={sliderData}
        showOverlay={true}
        overlayTitle=" Welcome to branché"
        overlayButtonText="Order now"
        overlayButtonHref="#"
        imageHeightClass="h-full"
      />
    </div>
  </section>

  <!-- Section groupe en pleine largeur -->
  <section class="bg-white py-8 px-4 text-center w-full">
    <h2 class="text-xl font-bold mb-6">
      Your Brunch Haven in the Heart of Leyton
    </h2>

    <!-- <div class="mx-auto mb-6 w-full max-w-5xl overflow-hidden rounded-lg"> -->
    <div class="flex justify-center mb-6">
      <div class="w-[400px] h-[200px] overflow-hidden rounded-3xl">
        <!-- Added rounded-lg here -->
        <Picture
          class="w-full h-full object-cover"
          src={GroupImage}
          widths={[400, 800, 1200, 1600]}
          sizes="(max-width: 1200px) 100vw, 1200px"
          formats={["avif", "webp"]}
          alt="Table pour un grand groupe avec plusieurs plats à partager"
          loading="lazy"
        />
      </div>
    </div>

    <div class="mx-auto mb-6 max-w-xl md:max-w-2xl">
      <p class="p">
        At branché, we bring the warmth and charm of the South of France to your
        table. Our cafe is a celebration of fresh, seasonal ingredients,
        thoughtfully prepared dishes, and a welcoming atmosphere that feels like
        home. Whether you’re here for a leisurely brunch, a quick coffee, or a
        moment of calm, we invite you to savor the simple joys of good food and
        great company..
      </p>
    </div>

    <a
      href="/private-events"
      class="inline-block py-4 px-6 bg-transparent border-2 border-black rounded-full text-black no-underline text-base mt-4 transition-all duration-300 hover:bg-white/20"
      >Large Group Booking Enquiry</a
    >
  </section>
</div>
